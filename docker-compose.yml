version: "3.8"

services:
  app:
    build:
      context: .
    env_file:
      - .env
    ports:
      - "${PORT:-8085}:8085"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    shm_size: '1gb'
  scheduler:
    build:
      context: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    # Run initial cache + Jellyfin sync, then repeat periodically
    command: sh -c "node src/scripts/updateCache.js || true; node scripts/sync-jellyfin.js || true; while true; do sleep ${SLEEP_SECONDS:-86400}; node src/scripts/updateCache.js || true; node scripts/sync-jellyfin.js || true; done"
